# =============================================================================
# PolyBuddy Environment Configuration
# =============================================================================
# Copy this file to .env and fill in required values.
# NEVER commit .env files with real credentials.
#
# Required variables are marked with [REQUIRED]
# Optional variables show their defaults in comments
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE [REQUIRED]
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Local default: postgresql://polybuddy:polybuddy@localhost:5432/polybuddy
DATABASE_URL=

# -----------------------------------------------------------------------------
# DOCKER COMPOSE (Optional)
# -----------------------------------------------------------------------------
# Override defaults for the PostgreSQL container
# Only needed if you want to change from defaults
#
# Default: polybuddy
POSTGRES_USER=
# Default: polybuddy
POSTGRES_PASSWORD=
# Default: polybuddy
POSTGRES_DB=
# Default: 5432
POSTGRES_PORT=

# -----------------------------------------------------------------------------
# API SERVER (Optional)
# -----------------------------------------------------------------------------
# Port for the Fastify API server
# Default: 3001
PORT=

# Host binding for API server
# Default: 0.0.0.0 (all interfaces - required for Docker)
HOST=

# CORS allowed origins (comma-separated URLs)
# Default: http://localhost:3000,http://localhost:3002,http://localhost:3003
CORS_ORIGIN=

# Cookie signing secret
# Default: polybuddy-secret-key-change-in-production
# IMPORTANT: Change this in production!
COOKIE_SECRET=

# -----------------------------------------------------------------------------
# WEB FRONTEND (Optional)
# -----------------------------------------------------------------------------
# API URL that the frontend uses to connect to the backend
# This is a NEXT_PUBLIC_ variable (exposed to browser)
# Default: http://localhost:3001
NEXT_PUBLIC_API_URL=

# -----------------------------------------------------------------------------
# INGESTION WORKER (Optional)
# -----------------------------------------------------------------------------
# Cron schedule for automatic market data sync
# Format: standard cron expression
# Default: */15 * * * * (every 15 minutes)
SYNC_INTERVAL=

# =============================================================================
# QUICK START VALUES (copy these to .env for local development)
# =============================================================================
# DATABASE_URL=postgresql://polybuddy:polybuddy@localhost:5432/polybuddy
# =============================================================================
